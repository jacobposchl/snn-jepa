# Paths (data_path must point to a Parquet from the dataset pipeline)
data_path:
results_out_path:
plots_out_path:

# Data split sizes (by session_id)
data:
  train_split:
  test_split:
  val_split:
  random_state:

# Model (required: model_config)
model_config:
  encoder_type:
  n_units:
  latent_dim:

# Training (required: training_config)
training_config:
  epochs:
  batch_size:
